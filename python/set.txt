set : 집합 자료형

* 대괄호

# 특징
1. 중복을 허용하지 않는다.
s = set("Hello") -> s : {'e','l','H','o'} 
2. 순서가 없다. 즉, 인덱싱 불가
2-1. 만약 인덱싱으로 접근하려면 list나 tuple로 변환해야 한다.

# 사용
1. add : 값 1개 추가
2. update : 값 여러개 추가
3. remove : 특정 값 제거
